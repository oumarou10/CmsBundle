{% extends 'OCCmsBundle::layout.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-xs-12 col-sm-8 col-md-6 col-lg-8">
               <h1> {{ page.title }} </h1>
                <p>{{ page.content|raw}} </p>
                <a href="{{ path('oc_cms_pages') }}"> Retour à la liste </a>
            </div>
        </div>
    </div>

{% endblock %}